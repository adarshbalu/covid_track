class CountryData {
  int totalConfirmed, totalRecovered, totalDeath, totalActive;
  String countryName, shortName;
  CountryData({
    this.totalConfirmed,
    this.totalDeath,
    this.totalRecovered,
    this.totalActive,
    this.countryName,
    this.shortName,
  });
}
